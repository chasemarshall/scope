// Simplified database interface - no longer needed since we moved to in-memory storage
// Keeping minimal exports for compatibility

console.log('Database module loaded - using in-memory storage');

export * from './schema';